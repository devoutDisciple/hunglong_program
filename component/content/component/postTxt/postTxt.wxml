<view bindtap="onClick" class="txt {{showAll ? '' : 'ellipsis' }}">
    <text class="link" wx:if="{{showLink && detail.title && type === 'posts'}}">#{{detail.title}}#</text>
    {{detail.desc}}
</view>