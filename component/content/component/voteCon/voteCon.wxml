<view class="vote_con">
    <view class="title">{{detail.title}}</view>
    <block wx:for="{{detail.content}}" wx:key="idx">
        <view class="vote_item">
            <vote-item detail="{{item}}" isSelect="{{false}}"/>
        </view>
    </block>
</view>