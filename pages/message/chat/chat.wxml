<view class="wrap">
    <scroll-view class="content" scroll-y="{{true}}">
        <chat-left wx:if="{{msg[0]}}" msg="{{msg[0]}}"/>
        <chat-right wx:if="{{msg[0]}}" msg="{{msg[0]}}"/>
        <!-- <block wx:for="{{msg}}" wx:key="{{index}}">
            <chat-item wx:if="{{item}}" msg="{{item}}"/>
        </block> -->
    </scroll-view>
    <view class="footer">
        <view class="desc">
            <input 
                class="ipt" 
                maxlength="200"
                value="{{msgTxt}}"
                placeholder="请输入" 
                cursor-spacing="180" 
                bindfocus="onInputFocus"
                bindinput="onChangeValue"
                confirm-type="send"/>
            <view class="send" bindtap = "onSendMsg">发送</view>
        </view>
        <view wx:if="{{focus}}" class="icon">
            <image class="icon_img" src="/asserts/public/img.png" />
            <image bindtap = "onShowEmoji" class="icon_img" src="/asserts/public/emoji.png" />
        </view>
        <scroll-view scroll-y="{{true}}" enable-flex wx:if="{{showEmoji}}" class="emoji">
            <block wx:for="{{emojis}}" wx:key="index">
                <view bindtap = "onClickEmoji" data-item="{{item}}" class="emoji_chunk">{{item}}</view>
            </block>
        </scroll-view>
    </view>
</view>